<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="LaptopPosApp.Views.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LaptopPosApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Login">

    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBox x:Name="UsernameTextbox"  Header="Username" Width="250" Text="{x:Bind ViewModel.Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <PasswordBox x:Name="PasswordBox" Header="Password" Width="250" Password="{x:Bind ViewModel.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <CheckBox Content="Remember me" IsChecked="{x:Bind ViewModel.RememberMe, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Margin="0, 10, 0, 0" Content="Login" Background="{StaticResource SystemAccentColor}" HorizontalAlignment="Stretch" Click="LoginButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
