<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LaptopPosApp.Views.ChangeTemporaryPricesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LaptopPosApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tableView="using:WinUI.TableView"
    mc:Ignorable="d"
>
    <Grid RowSpacing="10" Padding="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <tableView:TableView
            ItemsSource="{x:Bind ViewModel.TemporaryPrices}"
            SelectionUnit="Row"
            SelectionMode="Extended"
            AutoGenerateColumns="False"
            Grid.Row="0"
        >
            <tableView:TableView.Columns>
                <tableView:TableViewTextColumn Header="Ngày bắt đầu"
                    IsReadOnly="True"
                    Binding="{Binding StartDate, Mode=OneWay}"/>
                <tableView:TableViewTextColumn Header="Ngày kết thúc"
                    IsReadOnly="True"
                    Binding="{Binding EndDate, Mode=OneWay}"/>
                <tableView:TableViewTextColumn Header="Giá"
                    IsReadOnly="True"
                    Binding="{Binding Price, Mode=TwoWay}"/>
            </tableView:TableView.Columns>
        </tableView:TableView>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Spacing="5">
            <Button>
                <SymbolIcon Symbol="Add" />
            </Button>
            <Button>
                <SymbolIcon Symbol="Remove" />
            </Button>
        </StackPanel>
        <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Grid.Row="2">
            Thêm/Chỉnh sửa
        </TextBlock>
        <StackPanel Orientation="Vertical" Spacing="10" Grid.Row="3">
            <TextBlock>Thời gian bắt đầu</TextBlock>
            <StackPanel Orientation="Horizontal" Spacing="5">
                <CalendarDatePicker DateChanged="StartDate_DateChanged" Date="{Binding ViewModel.NewTemporaryPrice.StartDate, Mode=OneWay}" />
                <TimePicker TimeChanged="StartDate_TimeChanged" Time="{Binding ViewModel.NewTemporaryPrice.StartDate.TimeOfDay, Mode=OneWay}" />
            </StackPanel>
            <TextBlock>Thời gian kết thúc</TextBlock>
            <StackPanel Orientation="Horizontal" Spacing="5">
                <CalendarDatePicker DateChanged="EndDate_DateChanged" Date="{Binding ViewModel.NewTemporaryPrice.EndDate, Mode=OneWay}" />
                <TimePicker TimeChanged="EndDate_TimeChanged" Time="{Binding ViewModel.NewTemporaryPrice.EndDate.TimeOfDay, Mode=OneWay}" />
            </StackPanel>
            <TextBlock>Giá</TextBlock>
            <NumberBox Value="{Binding ViewModel.NewTemporaryPrice.Price, Mode=TwoWay}" Minimum="0" />
        </StackPanel>
    </Grid>
</Page>
